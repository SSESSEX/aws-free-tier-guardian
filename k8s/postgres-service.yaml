apiVersion: v1
kind: Service
metadata:
  name: guardian-postgres
  namespace: aws-guardian
spec:
  selector:
    app: guardian-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432